{#
/**
 * @file
 * Vartheme BS5 Rightup Starter Kit implementation for the Figure component.
 *
 * Stat figure card with a large number and description text. Background
 * and font sizes/colors are all configurable via Bootstrap utilities — no
 * custom CSS.
 *
 * - The number counts up from 0 when it scrolls into view (progressive
 *   enhancement: renders as the final number and is fully usable without
 *   JS; respects prefers-reduced-motion). See figure.js.
 *
 * Available properties:
 * - number: The main statistic number displayed prominently. (required)
 * - description: Short descriptive label below the number. (required)
 * - background_color: Brand background color applied to the card.
 *                     (none | bg-body-tertiary | bg-tertiary | bg-accent
 *                      | bg-accent-subtle | bg-primary | bg-primary-subtle
 *                      | bg-secondary | bg-secondary-subtle | bg-dark)
 *                     Defaults to `bg-accent-subtle`.
 * - align: Bootstrap text-* utility for the horizontal alignment of both
 *          the number and the description. (text-start | text-center
 *          | text-end) Defaults to `text-start`.
 * - number_size: Bootstrap display-* utility for the number.
 *                (display-1 | display-2 | display-3 | display-4
 *                 | display-5 | display-6) Defaults to `display-1`.
 * - number_color: Bootstrap text color for the number.
 *                 (text-body | text-primary | text-secondary | text-success
 *                  | text-danger | text-warning | text-info | text-light
 *                  | text-dark | text-muted | text-white | text-white-50
 *                  | text-black | text-black-50) Defaults to `text-black`.
 * - description_size: Bootstrap fs-* utility for the description.
 *                     (fs-1 | fs-2 | fs-3 | fs-4 | fs-5 | fs-6)
 *                     Defaults to `fs-6`.
 * - description_color: Bootstrap text color for the description label.
 *                      Same enum as number_color. Defaults to `text-black`.
 *
 * Available attributes:
 * - attributes: HTML attributes for the root element.
 * - number_attributes: HTML attributes for the number element.
 * - description_attributes: HTML attributes for the description element.
 */
#}

{# AI guidance:
 # - Do not alter property descriptions or existing logic unless new
 #   properties are added.
 # - Keep the structure: 1) variables, 2) logical variable manipulations,
 #   3) print the HTML.
 # - Always handle classes as arrays with `merge` (never string
 #   concatenation) to avoid duplicate classes and data type conflicts.
 #}

{# Variables: scalar props with schema defaults. #}
{% set background_color = background_color|default('bg-accent-subtle') %}
{% set background_color_class = (background_color != 'none') ? background_color : '' %}
{% set align            = align|default('text-start') %}
{% set number_size      = number_size|default('display-1') %}
{% set number_color     = number_color|default('text-black') %}
{% set description_size = description_size|default('fs-6') %}
{% set description_color = description_color|default('text-black') %}

{# Logic: root classes — Bootstrap utilities only, no custom CSS.
 # `align` lives on the root, not on the number/description elements: it's
 # one shared setting for both, and text-align is inherited, so setting it
 # once here aligns both children without duplicating the class. #}
{% set classes = [
  'figure',
  'd-flex',
  'flex-column',
  align,
  background_color_class,
  'p-4',
  'p-lg-5',
]|filter(v => v is not empty) %}

{# Logic: number and description class lists. #}
{% set number_classes = [number_size, number_color, 'lh-1'] %}
{% set description_classes = [description_size, description_color, 'mb-0', 'mt-2'] %}

{# Logic: per-element attributes objects for the nested number/description. #}
{% if not number_attributes %}
  {% set number_attributes = create_attribute() %}
{% endif %}
{% set number_attributes = number_attributes
  .addClass(number_classes)
  .setAttribute('data-count-target', number)
%}

{% if not description_attributes %}
  {% set description_attributes = create_attribute() %}
{% endif %}
{% set description_attributes = description_attributes.addClass(description_classes) %}

{# Print the HTML. #}
{% set attributes = attributes.addClass(classes) %}
<div{{ attributes }}>

  <span{{ number_attributes }}>{{ number|e }}</span>

  <p{{ description_attributes }}>{{ description|e }}</p>

</div>
